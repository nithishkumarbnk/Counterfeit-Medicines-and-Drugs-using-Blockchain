# backend/.env.example
# This file is for example purposes. Create a .env file locally and fill it with actual values.
# Add .env to your .gitignore!

# Ethereum provider URL (for local Ganache)
WEB3_PROVIDER_URL=http://127.0.0.1:8545

# Port for the Node.js backend
PORT=5000

# Example: MongoDB Atlas connection string (if you add a database later )
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/mydb?retryWrites=true&w=majority